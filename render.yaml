services:
  - type: web
    name: water-leakage-detection-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 60 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9